---
interface Props {
  text?: string;
  road?: boolean;
}

const { text, road } = Astro.props;
---

<h2 class:list={[road ? "absolute italic top-12 right-28 text-2xl font-bold mb-12" : "absolute italic top-16 right-32 text-2xl font-bold mb-12" ]}>
  {text}
</h2>
