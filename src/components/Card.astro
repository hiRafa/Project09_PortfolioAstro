<div class="h-fit col-start-3 col-span-9">
    <a class="relative w-full block" href={project?.data.projectURL} target="_blank">
      <img src={`/Images/${project?.data.thumbnail}.webp`} class="" />
      <ul
        class={`absolute bottom-0 right-0 font-bold py-4 w-full  text-right css-glass-bg px-6`}
      >
        {
          project?.data.projectBulletList.map((option: string) => (
            <li>❍ {option}</li>
          ))
        }
      </ul>
    </a>
  </div>