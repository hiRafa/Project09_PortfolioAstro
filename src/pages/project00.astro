---
import { Image } from "astro:assets";
import ProjectLayout from "../components/ProjectLayout.astro";

import { getEntry } from "astro:content";
const oldportfolio = await getEntry("projects", "oldportfolio");
const { Content, headings } = await oldportfolio.render();

---

<ProjectLayout project={oldportfolio}>
    <!-- <Image
      src="/Images/project_oldportfolio01.jpg"
      width={1100}
      height={940}
      format="jpg"
      alt="web development applications icons and logos"
      class="m-auto w-full max-lg:max-w-3xl max-lg:mr-0 max-md:w-6/6"
    />
    <Image
      src="/Images/project_oldportfolio02.jpg"
      width={1100}
      height={940}
      format="jpg"
      alt="web development applications icons and logos"
      class="m-auto w-full max-lg:max-w-3xl max-lg:mr-0 max-md:w-6/6"
    />
    <Image
      src="/Images/project_oldportfolio03.jpg"
      width={1100}
      height={940}
      format="jpg"
      alt="web development applications icons and logos"
      class="m-auto w-full max-lg:max-w-3xl max-lg:mr-0 max-md:w-6/6"
    /> -->
</ProjectLayout>
