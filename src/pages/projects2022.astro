---
import ProjectLayout from "../components/ProjectLayout.astro";

import { getEntry } from "astro:content";
const oldportfolio = await getEntry("projects", "oldportfolio");
const project01 = await getEntry("projects", "project01");
const project02 = await getEntry("projects", "project02");
const project03 = await getEntry("projects", "project02");
const projects2022 = [oldportfolio, project01, project02, project03]
---

<ProjectLayout projects={projects2022}>
 
</ProjectLayout>
